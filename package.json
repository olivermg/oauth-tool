{
  "name": "clinq-bridge-front",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "start": "yarn compile && node dist/index",
    "watch": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' --no-cache src/index.ts",
    "compile": "./node_modules/typescript/bin/tsc",
    "clean": "rm -rf ./dist"
  },
  "dependencies": {
    "@types/express": "^4.16.0",
    "@types/node": "^10.12.8",
    "axios": "^0.18.0",
    "typescript": "^3.1.6"
  },
  "devDependencies": {
    "ts-node": "^7.0.1"
  }
}
